{
  "dependencies": {
    "dotenv": "^17.4.2",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  },
  "scripts": {
    "scan": "node scripts/build-manifests.js",
    "dev": "npm run scan && vite --port 5173 --open --clearScreen false > vite-port.log",
    "build": "npm run scan && vite build"
  }
}
