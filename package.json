{
  "name": "core-lab-react",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "//": "Sentinel Build Guard — prevents catastrophic breakage",

    "verify": "node scripts/verify-folders.js",

    "prebuild": "npm run verify && node scripts/build-manifests.js && node scripts/build-sound-manifest.js",

    "build": "vite build",

    "dev": "vite",

    "preview": "vite preview"
  },

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },

  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
