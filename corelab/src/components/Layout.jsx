// ============================================================
// Layout.jsx — Minimal Wrapper (GR1 Stable)
// ============================================================

export default function Layout({ children }) {
  return (
    <div className="layout-root">
      {children}
    </div>
  );
}
